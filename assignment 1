def repeat(x):
    _size=len(x)
    repeated=[]
    for i in range(len(x)):
        k=i+1
        for j in range(k,len(x)):
            if x[i]==x[j] and x[i] not in repeated:
                repeated.append(x[i])
    return repeated
list1=[1,2,1,5,4,5,7,3,9,6] 
print(repeat(list1))
